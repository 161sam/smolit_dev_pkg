# Pfade & Ports
WORKSPACE="$HOME/OpenHands_Workspace"
LM_BASE_URL="http://127.0.0.1:1234/v1"           # LM Studio/OpenAI-kompatibler Server
OH_PORT="3311"                                   # OpenHands GUI
SEQ_PORT="8811"                                  # MCP Sequential Thinking (SSE)
MEM_PORT="8812"                                  # MCP Memory (SSE)
BRIDGE_PORT="8815"                               # smolit_dev Claude-Bridge HTTP
MEM_FILE="$HOME/.mcp/memory/memory_shared.json"  # Memory-Store für MCP-Server

# Container-Images (bei Bedarf überschreiben)
OH_IMAGE="docker.all-hands.dev/all-hands-ai/openhands:0.54.0"
RUNTIME_IMAGE="docker.all-hands.dev/all-hands-ai/runtime:0.54.0-nikolaik"

# API-Keys (optional, falls nicht bereits via CLI konfiguriert)
CLAUDE_API_KEY=""   # wird als ANTHROPIC_API_KEY an die Bridge weitergegeben
CODEX_API_KEY=""    # wird als OPENAI_API_KEY an die Bridge weitergegeben

# Optional: System-Prompt & Binärpfad für Claude
CLAUDE_MD_PATH="$HOME/CLAUDE.md"  # zusätzliche System-Anweisungen
CLAUDE_BIN=""                     # z.B. /usr/local/bin/claude (leer = auto)

